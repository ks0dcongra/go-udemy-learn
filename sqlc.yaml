version: "1"
packages:
  - name: "db"
    path: "./db/sqlc/"
    queries: "./db/query/"
    schema: "./db/migration/"
    engine: "postgresql"
    emit_json_tags: true
    # 自動優化
    emit_prepared_queries: false
    emit_interface: false
    # Account -> Accounts
    emit_exact_table_names: false